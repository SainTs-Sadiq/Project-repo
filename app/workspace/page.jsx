"use client";
import {useEffect,useMemo,useState} from 'react';import{getSupabaseBrowser}from '../../lib/supabase/browser';
const constraints=['Peanut allergy','Dairy-free','Gluten-free','Vegetarian','Vegan','Low-sodium','Halal','Kosher'];const goals=['General wellness','Weight management','High protein','Heart health','Athletic performance'];
export default function Workspace(){const supabase=useMemo(()=>getSupabaseBrowser(),[]);const[session,setSession]=useState(null),[profile,setProfile]=useState({name:'',budget:100,goal:'General wellness',constraints:[]}),[orders,setOrders]=useState([]),[catalog,setCatalog]=useState([]),[cart,setCart]=useState([]),[view,setView]=useState('discover'),[profileOpen,setProfileOpen]=useState(false),[search,setSearch]=useState(''),[notice,setNotice]=useState(''),[loading,setLoading]=useState(true);
const meals=useMemo(()=>catalog.flatMap(menu=>(menu.menu_items||[]).map(item=>({id:item.id,menu_item_id:item.id,name:item.name,restaurant:menu.vendors?.name||'Vendor',vendor_id:menu.vendor_id,price:Number(item.price||0),ingredients:item.ingredients||[],allergens:item.allergens||[],tags:item.dietary_attributes||[],image:'🍽️'}))),[catalog]);const filtered=meals.filter(m=>`${m.name} ${m.restaurant} ${m.tags.join(' ')}`.toLowerCase().includes(search.toLowerCase()));const total=cart.reduce((s,x)=>s+x.price,0);
useEffect(()=>{let live=true;(async()=>{const{data}=await supabase.auth.getSession();if(!live)return;setSession(data.session);if(data.session)await load(data.session);else setLoading(false)})();const{data:l}=supabase.auth.onAuthStateChange(async(_,s)=>{setSession(s);if(s){setLoading(true);await load(s);setLoading(false)}else setOrders([])});return()=>{live=false;l.subscription.unsubscribe()}},[supabase]);
async function load(s){const h={Authorization:`Bearer ${s.access_token}`};const[p,o,c]=await Promise.all([fetch('/api/profile',{headers:h}),fetch('/api/orders',{headers:h}),fetch('/api/catalog')]);const pd=await p.json(),od=await o.json(),cd=await c.json();if(pd.profile)setProfile({name:pd.profile.display_name||'',budget:Number(pd.profile.weekly_budget||0),goal:pd.profile.nutrition_goal||'General wellness',constraints:pd.profile.constraints||[]});setOrders(od.orders||[]);setCatalog(cd.menus||[])}
function notify(x){setNotice(x);setTimeout(()=>setNotice(''),2500)}async function saveProfile(next){setProfile(next);if(!session)return;const r=await fetch('/api/profile',{method:'POST',headers:{Authorization:`Bearer ${session.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({profile:next})});if(!r.ok)notify('Profile could not be saved.')}function toggle(c){saveProfile({...profile,constraints:profile.constraints.includes(c)?profile.constraints.filter(x=>x!==c):[...profile.constraints,c]})}function add(item){if(profile.constraints.some(c=>item.allergens?.some(a=>a.toLowerCase().includes(c.split(' ')[0].toLowerCase())))){notify('This item has a potential profile conflict. Review the item before procurement.');return}setCart(c=>[...c,item]);notify(`${item.name} added to procurement cart.`)}
async function checkout(){if(!cart.length)return;const vendors=[...new Set(cart.map(x=>x.vendor_id))];if(vendors.length!==1){notify('Procurement must contain items from one vendor.');return}const r=await fetch('/api/orders',{method:'POST',headers:{Authorization:`Bearer ${session.access_token}`,'Content-Type':'application/json'},body:JSON.stringify({items:cart.map(x=>({menu_item_id:x.menu_item_id,quantity:1}))})});const d=await r.json();if(!r.ok){notify(d.error||'Procurement failed.');return}setCart([]);await load(session);notify('Procurement request submitted.');setView('orders')}
if(loading)return <div className="loading">Loading your procurement workspace…</div>;return <div className="workspace"><style>{css}</style><header><button className="brand" onClick={()=>setView('discover')}><b>IF</b><span>Intelligent Food<small>PROCUREMENT SYSTEM</small></span></button><nav>{[['discover','Discover'],['for-you','For You'],['cart',`Cart${cart.length?` (${cart.length})`:''}`],['orders','Orders'],['vendor','Vendor AI']].map(([v,l])=><button className={view===v?'active':''} onClick={()=>setView(v)} key={v}>{l}</button>)}</nav><div className="user"><button onClick={()=>setProfileOpen(true)}>{(profile.name||session?.user?.email||'U')[0].toUpperCase()}</button><button onClick={()=>supabase.auth.signOut()}>Sign out</button></div></header>{notice&&<div className="notice">✓ {notice}</div>}{view==='discover'&&<Page title="Discover meals" eyebrow="LIVE VENDOR CATALOGUE"><input className="search" value={search} onChange={e=>setSearch(e.target.value)} placeholder="Search published meals, vendors or dietary attributes…"/>{filtered.length?<div className="cards">{filtered.map(m=><Meal key={m.id} m={m} add={()=>add(m)}/>)}</div>:<div className="panel"><h2>No published meals yet</h2><p>Once an approved vendor publishes an active menu, its items will appear here.</p></div>}</Page>}{view==='for-you'&&<Page title="For your profile" eyebrow="PERSONALIZED RECOMMENDATIONS"><div className="profile-summary"><div><small>CONSTRAINTS</small><b>{profile.constraints.length?profile.constraints.join(' · '):'No restrictions set'}</b></div><div><small>GOAL</small><b>{profile.goal}</b></div><div><small>BUDGET</small><b>${profile.budget}/week</b></div><button onClick={()=>setProfileOpen(true)}>Edit profile</button></div><div className="cards">{filtered.map(m=><Meal key={m.id} m={m} add={()=>add(m)}/>)}</div></Page>}{view==='cart'&&<Page title="Procurement cart" eyebrow="READY TO PROCURE"><div className="panel">{cart.map((m,i)=><div className="line" key={i}><span>{m.image}</span><div><b>{m.name}</b><small>{m.restaurant}</small></div><strong>${m.price.toFixed(2)}</strong></div>)}{!cart.length&&<p>Your cart is empty.</p>}<div className="total"><b>Total</b><strong>${total.toFixed(2)}</strong></div><button className="primary" disabled={!cart.length} onClick={checkout}>Submit procurement →</button></div></Page>}{view==='orders'&&<Page title="Your orders" eyebrow="PERSISTENT PROCUREMENT HISTORY">{orders.length?orders.map(o=><div className="order" key={o.id}><b>#{o.id.slice(0,8)}</b><span>{o.procurement_items?.length||0} items</span><strong>${Number(o.total).toFixed(2)}</strong><em>{o.status}</em></div>):<div className="panel">No procurement orders yet.</div>}</Page>}{view==='vendor'&&<Page title="AI menu intelligence" eyebrow="RESTAURANT / VENDOR PORTAL"><div className="panel"><p>Vendor AI remains available through the operational dashboard.</p><a href="/vendor">Open vendor operations →</a></div></Page>}{profileOpen&&<div className="overlay"><div className="modal"><button className="x" onClick={()=>setProfileOpen(false)}>×</button><span>YOUR PROFILE</span><h2>Dietary preferences</h2><label>Name<input value={profile.name} onChange={e=>setProfile({...profile,name:e.target.value})}/></label><label>Weekly budget<input type="number" value={profile.budget} onChange={e=>setProfile({...profile,budget:Number(e.target.value)})}/></label><label>Goal</label><div className="options">{goals.map(g=><button className={profile.goal===g?'selected':''} onClick={()=>saveProfile({...profile,goal:g})} key={g}>{g}</button>)}</div><label>Constraints & allergies</label><div className="options">{constraints.map(c=><button className={profile.constraints.includes(c)?'selected':''} onClick={()=>toggle(c)} key={c}>{profile.constraints.includes(c)?'✓ ':''}{c}</button>)}</div><button className="primary" onClick={()=>setProfileOpen(false)}>Done</button></div></div>}</div>}
function Page({title,eyebrow,children}){return <main><div className="title"><span>{eyebrow}</span><h1>{title}</h1></div>{children}</main>}function Meal({m,add}){return <article className="meal"><div className="emoji">{m.image}</div><div className="mealbody"><small>{m.restaurant}</small><h3>{m.name}</h3><div className="tags">{m.tags.map(t=><span key={t}>{t}</span>)}</div><p>{m.ingredients.length?m.ingredients.join(' · '):'Ingredients not supplied.'}</p><div className="mealfoot"><strong>${m.price.toFixed(2)}</strong><button onClick={add}>+ Add</button></div></div></article>}
const css=`:root{--bg:#f5f3ee;--paper:#faf9f5;--ink:#191914;--muted:#77766d;--line:#dedbd2;--green:#314d3b;--gold:#b08b35;--cream:#ebe6d8}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font-family:Arial,sans-serif}button,input{font:inherit}.workspace{min-height:100vh}header{height:76px;background:var(--paper);border-bottom:1px solid var(--line);display:flex;align-items:center;padding:0 5vw;gap:28px;position:sticky;top:0;z-index:20}.brand{border:0;background:none;display:flex;align-items:center;gap:10px;cursor:pointer;text-align:left}.brand b{width:36px;height:36px;border-radius:50%;background:var(--green);color:#fff;display:grid;place-items:center}.brand span{font:700 15px Georgia}.brand small{display:block;font:7px Arial;letter-spacing:2px;color:#888}nav{display:flex;gap:4px;margin:auto}nav button,.user button{border:0;background:none;padding:10px;color:#777;font-size:10px;cursor:pointer}nav .active{color:var(--green);font-weight:700}.user{display:flex;gap:4px}.user button:first-child{width:32px;height:32px;border-radius:50%;background:var(--cream);color:var(--green);font-weight:700}main{max-width:1160px;margin:auto;padding:70px 5vw}.title{border-bottom:1px solid var(--line);padding-bottom:28px;margin-bottom:25px}.title span{font-size:8px;letter-spacing:2px;color:var(--gold);font-weight:700}.title h1{font:700 55px Georgia;margin:12px 0}.search{width:100%;padding:15px;background:var(--paper);border:1px solid var(--line);margin-bottom:22px}.cards{display:grid;grid-template-columns:repeat(3,1fr);gap:16px}.meal,.panel,.order,.profile-summary{background:var(--paper);border:1px solid var(--line)}.meal{overflow:hidden}.emoji{height:165px;background:var(--cream);display:grid;place-items:center;font-size:65px}.mealbody{padding:17px}.mealbody small{font-size:8px;color:#888}.meal h3{font:700 18px Georgia;margin:7px 0}.meal p,.panel p{font-size:10px;color:var(--muted);line-height:1.6}.tags,.options{display:flex;flex-wrap:wrap;gap:6px;margin:12px 0}.tags span{background:var(--cream);padding:5px 7px;font-size:7px}.mealfoot,.total{display:flex;justify-content:space-between;align-items:center;border-top:1px solid var(--line);padding-top:13px;margin-top:15px}.mealfoot strong,.total strong{color:var(--green)}.mealfoot button{border:0;background:none;color:var(--green);font-size:9px;font-weight:700;cursor:pointer}.profile-summary{display:grid;grid-template-columns:1.4fr 1fr .7fr auto;gap:15px;padding:18px;margin-bottom:25px}.profile-summary small,.profile-summary b{display:block}.profile-summary small{font-size:7px;color:#888}.profile-summary b{font-size:9px;margin-top:7px}.profile-summary button{background:none;border:1px solid var(--line);padding:9px;font-size:8px}.panel{padding:25px}.line{display:grid;grid-template-columns:55px 1fr auto;gap:14px;align-items:center;padding:14px 0;border-bottom:1px solid var(--line)}.line span{font-size:30px}.line small,.line b{display:block}.line small{font-size:8px;color:#888}.total{margin:20px 0}.primary{border:0;background:var(--green);color:#fff;padding:14px 20px;font-size:9px;cursor:pointer}.primary:disabled{opacity:.5}.order{display:grid;grid-template-columns:1fr 1fr 1fr auto;gap:15px;padding:18px;margin-bottom:10px;font-size:9px}.order strong{color:var(--green)}.order em{background:var(--cream);padding:6px 8px;font-style:normal}.notice{position:fixed;right:20px;top:90px;background:var(--green);color:#fff;padding:12px 17px;font-size:9px;z-index:50}.overlay{position:fixed;inset:0;background:#18201999;z-index:100;display:grid;place-items:center;padding:20px}.modal{width:min(600px,100%);max-height:90vh;overflow:auto;background:var(--paper);padding:30px;position:relative}.modal>span{font-size:8px;letter-spacing:2px;color:var(--gold)}.modal h2{font:700 30px Georgia}.modal label{display:block;font-size:8px;margin:17px 0 7px}.modal input{display:block;width:100%;padding:12px;border:1px solid var(--line);margin-top:7px}.options button{border:1px solid var(--line);background:#fff;padding:9px;font-size:8px;cursor:pointer}.options .selected{background:var(--green);color:#fff}.x{position:absolute;right:15px;top:10px;border:0;background:none;font-size:25px}.loading{min-height:100vh;display:grid;place-items:center;font:16px Georgia;color:var(--green)}@media(max-width:850px){nav{display:none}.cards{grid-template-columns:1fr 1fr}.profile-summary{grid-template-columns:1fr 1fr}}@media(max-width:560px){main{padding:45px 20px}.cards,.profile-summary{grid-template-columns:1fr}.title h1{font-size:43px}}`;
