"use client";
import{useEffect,useState}from'react';import{getSupabaseBrowser}from'../../lib/supabase/browser';
export default function Admin(){const s=getSupabaseBrowser(),[vendors,setVendors]=useState([]),[menus,setMenus]=useState([]),[msg,setMsg]=useState(''),[ok,setOk]=useState(false);async function load(){const{data:{session}}=await s.auth.getSession();if(!session){setMsg('Sign in with an administrator account.');return}const h={Authorization:`Bearer ${session.access_token}`};const[v,m]=await Promise.all([fetch('/api/admin/vendors',{headers:h}),fetch('/api/admin/menus',{headers:h})]);const vd=await v.json(),md=await m.json();if(!v.ok){setMsg(vd.error||'Access denied.');return}setOk(true);setVendors(vd.vendors||[]);setMenus(md.menus||[])}useEffect(()=>{load()},[]);async function patch(url,body){const{data:{session}}=await s.auth.getSession();const r=await fetch(url,{method:'PATCH',headers:{Authorization:`Bearer ${session.access_token}`,'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json();setMsg(r.ok?'Saved.':d.error||'Update failed.');if(r.ok)load()}if(!ok&&msg)return <main className="admin"><style>{css}</style><h1>Admin access</h1><p>{msg}</p></main>;return <main className="admin"><style>{css}</style><header><span>PHASE 5C · GOVERNANCE</span><h1>Approval console.</h1><p>Review vendor onboarding and control which menus become visible to customers.</p></header>{msg&&<div className="notice">{msg}</div>}<section><h2>Vendor onboarding</h2>{vendors.map(v=><div className="row" key={v.id}><div><b>{v.name}</b><small>{v.contact_email||'No email'} · {v.phone||'No phone'}</small></div><em>{v.status}</em><div>{v.status!=='Active'&&<button onClick={()=>patch('/api/admin/vendors',{vendor_id:v.id,status:'Active'})}>Activate</button>}{v.status==='Active'&&<button onClick={()=>patch('/api/admin/vendors',{vendor_id:v.id,status:'Suspended'})}>Suspend</button>}</div></div>)}</section><section><h2>Menu publishing</h2>{menus.map(m=><div className="row" key={m.id}><div><b>{m.name}</b><small>{m.vendors?.name||'Unknown vendor'} · {m.menu_items?.length||0} items</small></div><em>{m.status}</em><div>{m.status==='Draft'&&<button disabled={m.vendors?.status!=='Active'} onClick={()=>patch('/api/admin/menus',{menu_id:m.id,status:'Published'})}>{m.vendors?.status==='Active'?'Publish':'Vendor inactive'}</button>}{m.status==='Published'&&<button onClick={()=>patch('/api/admin/menus',{menu_id:m.id,status:'Archived'})}>Archive</button>}</div></div>)}</section></main>}
const css=`body{margin:0;background:#f5f3ee;color:#181813;font-family:Arial}.admin{max-width:1100px;margin:auto;padding:70px 5vw}.admin header{border-bottom:1px solid #dedbd2;padding-bottom:35px}.admin header span{font-size:8px;letter-spacing:2px;color:#b08b35;font-weight:700}.admin h1{font:700 54px Georgia;margin:12px 0}.admin header p,.admin small{font-size:10px;color:#777;line-height:1.6}.admin section{background:#faf9f5;border:1px solid #dedbd2;margin-top:20px;padding:24px}.admin h2{font:700 26px Georgia}.row{display:grid;grid-template-columns:1fr auto auto;gap:18px;align-items:center;border-top:1px solid #dedbd2;padding:16px 0}.row b,.row small{display:block}.row small{font-size:8px}.row em{background:#ebe6d8;padding:6px 8px;font-size:8px;font-style:normal}.row button{border:0;background:#314d3b;color:#fff;padding:9px;font-size:8px;cursor:pointer}.row button:disabled{opacity:.45;cursor:not-allowed}.notice{position:fixed;top:20px;right:20px;background:#314d3b;color:#fff;padding:12px 18px;font-size:9px}@media(max-width:650px){.row{grid-template-columns:1fr}.admin h1{font-size:42px}}`;
